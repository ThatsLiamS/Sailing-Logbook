{
	"name": "sailing-logbook",
	"version": "0.1.0",
	"scripts": {
		  "test": "echo \"Error: no test specified\" && exit 1"
	},
	"dependencies": {
		  "bootstrap": "^5.1.3"
	},
	"devDependencies": {
		  "eslint": "^8.16.0"
	}
}
